# MD SafeEdit 项目总览

## 一句话介绍

让 AI 准确修改 Markdown 文档中的指定内容，并在文档已被人或其他 Agent 修改时拒绝静默覆盖。

## 1. 项目要解决什么问题

AI Agent 已经可以通过全文覆写、字符串替换和 Diff 修改 Markdown，但这些方式没有统一回答三个问题：

1. Agent 修改的是否还是它刚才读到的那份文档？
2. 目标段落、章节或表格行是否已经被别人修改？
3. 如果目标只是被移动了，系统能否安全地重新找到它？

MD SafeEdit 为 Agent 提供一套 Markdown-aware 的安全编辑能力：

- 用章节、段落、列表项、表格行等结构帮助定位；
- Agent 读取目标时获得一个不可拆分的 Anchor；
- 写入前校验文档版本和目标原始内容；
- 目标未变但位置移动时，可以进行精确重定位；
- 目标已变、消失或无法唯一确认时，返回冲突；
- 未修改的内容保持字节级不变。

## 2. 核心产品原则

### 2.1 不追求“所有修改都成功”

项目最重要的指标不是自动编辑成功率，而是错误放行率。

当系统无法确认目标时，正确行为是拒绝修改，而不是选择最相似的内容继续写入。

### 2.2 Markdown 结构用于定位，不用于覆盖授权

相同标题、相似段落和重复模板都可能出现。结构路径只能帮助缩小候选范围。

只有目标原始字节仍然一致，才能在第一版中自动重新定位并写入。

### 2.3 不污染源文档

第一版不向 Markdown 文件写入永久 Block ID，不要求用户迁移到新的文档格式。

### 2.4 与普通编辑工具共存

MD SafeEdit 不需要替代所有 Edit 或 Patch 工具。

- 创建新文件：直接写文件；
- 修改短小且唯一的文本：字符串替换；
- 复杂通用文本变更：Diff；
- 长期维护、重复结构、并发修改敏感：MD SafeEdit。

## 3. 产品组成

```text
MD SafeEdit
├── Guarded Patch Core
│   ├── 文档快照与版本
│   ├── Anchor 校验
│   ├── Batch 与范围冲突检测
│   ├── Compare-and-Swap
│   └── 原子写入
│
├── Markdown Structural Adapter
│   ├── Markdown 解析
│   ├── Outline 与 Section
│   ├── Search 与 Read
│   ├── 结构证据
│   └── 精确重定位
│
├── Protocol
│   ├── 请求与响应 Schema
│   ├── Anchor Token
│   └── 稳定错误码
│
├── CLI
├── MCP Server
└── Benchmark
```

## 4. 面向 Agent 的工具

第一版只暴露四个主要工具。

### `inspect`

查看文档版本、方言和标题大纲。

### `search`

按文本、节点类型和章节范围搜索。

### `read`

读取目标节点和必要上下文，同时获得 Anchor Token。

### `patch`

使用 Anchor 执行替换、插入、删除或批量修改，并支持 Dry Run。

任何写操作都不能只提交临时 `node_id`，必须携带由 `read` 返回的 Anchor。

## 5. 一期、二期及后续范围

## 一期：Guarded Patch MVP

目标：先证明“不静默覆盖”能够可靠实现。

包括：

- CommonMark 与 GFM 基础解析；
- Section、Paragraph、List Item、Table Row；
- Outline、Search、Read；
- 文档 Revision；
- Raw Content Hash；
- 不可伪造的 Anchor Token；
- Replace、Insert、Delete；
- 原子 Batch；
- Dry-run Diff；
- 同版本写入校验；
- 原子落盘；
- CLI JSON 接口。

一期不做：

- 文件变化后的自动重定位；
- MCP；
- Obsidian 特殊语法；
- 模糊匹配；
- GUI。

预计周期：2～3周，另加3～5天技术可行性验证。

## 二期：精确重定位与 MCP

目标：证明 Markdown 结构相对通用 Hash Patch 的额外价值。

包括：

- 文件其他区域变化后的目标重定位；
- 重名标题与重复节点处理；
- 父节点、前后邻居等结构证据；
- 唯一性和歧义检测；
- MCP Server；
- 面向人的 CLI；
- 第一版 Benchmark；
- 不同模型的工具调用测试。

预计周期：2～3周。

## 三期：Benchmark 与工程化

目标：公开证明项目在哪些场景更好，哪些场景没有必要使用。

包括：

- 200个以上公开任务；
- 与全文覆写、字符串替换、Unified Diff、行级 Hash Patch 对比；
- Token、延迟、成功率和安全指标；
- Windows、macOS、Linux；
- npm 发布；
- 示例集成；
- 公开 Benchmark 报告。

预计周期：2～4周。

## 后续

根据真实需求选择：

- Obsidian Block ID 与 Callout；
- Frontmatter 字段级编辑；
- MDX 读取；
- 编辑器插件；
- GitHub Action；
- Anchor 跨重启持久化；
- 需要确认的格式等价重定位。

## 6. 衡量产品好坏的方法

### 第一优先级：安全性

#### 错误放行率

目标已经变化或存在歧义时，系统仍然写入的比例。

目标：Benchmark 中为 **0%**。

#### 错误目标率

修改成功但写到了错误节点的比例。

目标：**0%**。

#### 歧义拒绝率

故意构造多个相同候选时，系统是否拒绝猜测。

目标：**100%**。

### 第二优先级：有效性

#### 有效编辑成功率

在安全前置条件满足时，是否正确完成编辑。

Beta 目标：**不低于95%**。

#### 精确重定位成功率

目标未变但位置发生变化时，能否安全找回。

目标：**不低于95%**。

### 第三优先级：无损性

未修改范围是否保持字节级一致。

目标：**100%**。

### 第四优先级：Agent 可用性

- Agent 是否知道先 Read 再 Patch；
- 是否能原样返回 Anchor；
- 是否能根据错误码恢复；
- 是否会频繁错误选择工具；
- 不同模型之间是否稳定。

### 第五优先级：效率

- 输入 Token；
- 输出 Token；
- Tool Call 数量；
- 重试次数；
- 端到端延迟；
- 解析和重定位耗时。

Token 节省是加分项，不是核心成立条件。

## 7. 项目是否值得继续的阶段门槛

### Gate A：解析可行

Markdown Parser 必须能够给出可靠的原始字节范围，否则停止并更换技术路线。

### Gate B：安全写入成立

目标变化、Batch 冲突和写入失败时，不能破坏原文件。

### Gate C：结构化重定位有价值

必须证明结构相对通用行级 Hash Patch 能提高正确率或恢复能力。

### Gate D：Agent 会用

不同模型必须能稳定理解四个工具和冲突恢复流程。

如果某个 Gate 失败，应缩小项目，而不是继续增加复杂功能。

## 8. 文档导航

- `README.md`：面向开源用户的项目首页；
- `AGENTS.md`：编程 Agent 必须遵守的实现规则；
- `docs/01-PRD.md`：完整产品需求；
- `docs/02-ARCHITECTURE.md`：系统架构；
- `docs/03-PROTOCOL.md`：Agent 接口和协议；
- `docs/04-DEVELOPMENT.md`：实现顺序与工程规范；
- `docs/05-ROADMAP.md`：周期、分期和阶段门槛；
- `docs/06-TESTING-AND-EVALUATION.md`：测试集、指标和评估方法；
- `docs/07-RESEARCH-AND-ALTERNATIVES.md`：同类方案与替代关系；
- `docs/08-DECISIONS-AND-OPEN-QUESTIONS.md`：已确认决策和待决问题。

